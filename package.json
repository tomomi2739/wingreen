{
  "name": "wingreen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "合同会社WinGreen 会計・請求書管理システム",
  "scripts": {
    "dev": "node --watch-path=./api --watch-path=./lib --watch-path=./config --watch-path=./scripts scripts/dev-server.mjs",
    "schema": "node scripts/notion-schema.mjs",
    "dump": "node scripts/notion-dump.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1"
  }
}
