# WinGreen 会計・請求書管理システム

## プロジェクト概要
合同会社WinGreen（競輪選手のマネジメント・ライブ配信投げ銭・イベント出演を手がける会社）向けの、
Notion連携型の会計ダッシュボード + 請求書発行アプリ。PC専用（モバイル対応・PWA化は不要）。

## 技術構成
- Vercel serverless functions（`/api` 配下）+ 静的HTML（`/public` 配下）
- Notion API経由でデータ取得・書き込み
- Vercel Hobbyプランのため、サーバーレス関数は12個までの制限あり。
  `/api` に新規関数を追加する前に必ず `ls api/` で現在の数を確認すること。

## 絶対に守ること
- NotionのDB構造・プロパティ名・IDを勝手に推測しない。必ず実際のデータを確認してから
  コードを書くこと。不明な点はユーザーに質問する。
- Notionの操作（DB作成・プロパティ追加など）はユーザー自身が行う。Claude Codeは
  コードの実装のみを担当する。
- コードはファイルとして明確に「新規作成」か「既存の上書き」かを分かりやすく示すこと。
- git操作（commit・push）は、ユーザーから明示的に指示されたときのみ実行する。

## Notionデータベース一覧（すべて末尾 "WG"）
- 勘定科目マスタ WG
- 補助科目マスタ WG
- 支払先マスタ WG（選手・外注先）
- 取引先マスタ WG（顧客・請求先）
- 仕訳明細 WG（本体、借方勘定科目／借方補助科目／貸方勘定科目／貸方補助科目の構成）
- 固定費マスタ WG
- 請求書 WG（親、IDプロパティで自動採番）
- 請求書明細 WG（子）

## 環境変数
- `NOTION_TOKEN_WG`：WinGreen専用のNotionインテグレーショントークン
- 各DBのIDは、確認でき次第 `.env` や設定ファイルにまとめる（まだ未設定）

## 作りたい機能（優先順）
1. 月次レポート画面：売上・売上内訳・利益・経費内訳を表とグラフで表示
2. 請求書発行機能：請求書WG／請求書明細WGのデータからPDFを生成
3. （将来）資金繰り予測：固定費マスタ + 仕訳明細から将来のキャッシュフローを試算
